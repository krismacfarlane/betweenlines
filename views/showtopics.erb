<h1> <%= @title %> </h1>
  <a href="/:title/:topic/messages/new">add a new message</a>
  <ul>
    <% @bookshelf.each do |id| %>
    <li><%= $bookshelf.hget("book#{id}", "topic_name") %></li>
    <% end %>
  </ul>
